var React = require('react');
var ReactDOM = require('react-dom');
var LocationManager = require('./components/LocationManager.jsx');

ReactDOM.render(<LocationManager />, document.getElementById('weather'));
